machine:
  node:
    version: 6.1.0

dependencies:
  pre:
    - npm install -g bower polymer-cli
    - bower install
  post:
    - bower ls

test:
  override:
    - polymer test --expanded

experimental:
  notify:
    branches:
      only:
        - master
